# Optional: existing Windows user environment variables are also supported.
OPENROUTER_API_KEY=
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
# GREATSAGE_DATA_DIR defaults to %LOCALAPPDATA%/GreatSage for CLI usage.
# The development desktop uses .runtime/ within the project.
